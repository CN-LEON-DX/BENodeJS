mixin filter-status(filterStatus)    
    each filter in filterStatus
        button(
            class=`btn btn-sm ml-1 btn-outline-success ${filter.class}` style="margin-right: 10px"
            button-status = filter.status
        ) #{filter.name}
    